{{- if .Values.dashboard.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "netbird.dashboard.envSecret" . }}
  labels:
    {{- include "netbird.dashboard.labels" . | nindent 4 }}
stringData:
  {{- toYaml .Values.dashboard.env | nindent 2 }}
{{- end }}
