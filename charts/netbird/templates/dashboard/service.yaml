{{- if .Values.dashboard.enabled }}
kind: Service
apiVersion: v1
metadata:
  name: {{ include "netbird.dashboard.deploymentName" . }}
  labels:
    {{- include "netbird.dashboard.labels" . | nindent 4 }}
  {{- with .Values.dashboard.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
  - name: http
    port: {{ .Values.dashboard.service.httpPort }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "netbird.dashboard.selectorLabels" . | nindent 4 }}
{{- end }}
